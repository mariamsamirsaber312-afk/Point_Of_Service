import 'package:flutter/material.dart';

class AppColor{

// Backgrounds
static const Color primaryBG = Color.fromRGBO(104, 84, 152, 1);
static const Color secondaryBG = Colors.white;
static const Color selectedBtnBG = Color.fromRGBO(255, 255, 255, 0.2);
static const Color searchBarBG = Color.fromRGBO(226, 232, 240, 1);
static const Color paymentViewBG = Color.fromRGBO(230, 230, 230, 0.2);

//  Text Colors
  static const Color textPrimary = Color.fromRGBO(104, 84, 152, 1);
  static const Color textSecondary = Colors.white;
  static const Color posAddress = Color.fromRGBO(31, 31, 31, 1);


  // Text Field Color
  static const Color textField = Color.fromRGBO(31, 31, 31, 1);
  static const Color searchTextField = Color.fromRGBO(169, 169, 169, 1);

  //border
  static const Color keyboardBtnBorder = Color.fromRGBO(0, 0, 0, 0.1);

}